#include "MainScene.h"

namespace nadpher
{
	void MainScene::handle_events(sf::Event& event)
	{
		switch (event.type)
		{
		default:
			break;
		}
	}

	void MainScene::iterate(float deltaTime)
	{

	}

	void MainScene::draw(sf::RenderTarget& target, sf::RenderStates states) const
	{

	}
}